#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t,n,i;
    
    cin>>t;
    
    while(t--){
        
        cin>>n;
        
        cout<<"1 1";
        
        for(i=0;i<(n/2);i++) cout<<"0";
        
        cout<<endl;
    
    }
}
